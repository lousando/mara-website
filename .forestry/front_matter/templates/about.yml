---
label: About
hide_body: true
fields:
- type: field_group_list
  name: officers
  label: Club Officers
  fields:
  - type: text
    name: name
    label: Name
    config:
      required: true
  - type: text
    name: call_sign
    label: Call Sign
    config:
      required: false
  - type: text
    name: role
    label: Role
    description: President, Vice President, etc...
  - type: file
    name: image_src
    label: Image
  - type: textarea
    name: bio
    label: Biography
    description: A short note about the person.
  config:
    labelField: name
- type: field_group_list
  name: board_members
  label: Club Board Members
  fields:
  - type: text
    name: name
    label: Name
    config:
      required: true
  - type: text
    name: call_sign
    label: Call Sign
  - name: image_src
    type: file
    config:
      maxSize: 
    label: Image
  config:
    labelField: name
  description: Order here is not preserved. Members are sorted alphabetically on website.
- type: field_group_list
  name: ve_team
  label: Club VE Team
  fields:
  - type: text
    name: name
    label: Name
    config:
      required: true
  - type: text
    name: call_sign
    label: Call Sign
  - name: image_src
    type: file
    config:
      maxSize: 
    label: Image
  config:
    labelField: name
  description: Order here is not preserved. Members are sorted alphabetically on website.
